public class TazzaPerDadi {
    private Dado[] arr;

    TazzaPerDadi(int nMax) {
        arr = new Dado[nMax];
    }

    TazzaPerDadi(int N, int F) {
        arr = new Dado[N];
        for (int i = 0; i < N; i++) {
            arr[i] = new Dado(F);
        }
    }

    public boolean add(Dado d) {
        if (arr)
    }
}
